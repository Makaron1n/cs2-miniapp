# CS2 Case Opening Mini-App

## 📋 Описание проекта

Мини-приложение для открытия кейсов CS2 в стиле Telegram Web App. Включает:
- Анимированную карусель с рандомизацией
- Систему редкостей скинов
- Инвентарь с продажей/выводом
- Систему ежедневных квестов
- Админ-панель для создания кейсов
- Фейковую статистику CS2

---

## 📁 Структура проекта

```
cs2-miniapp/
├── index.html              # Главная страница (точка входа)
├── app.bundle.js           # Основная логика (все классы ядра)
├── styles.css              # Основные стили
├── skins.json              # База данных скинов (2000+ скинов)
├── README.md               # Краткое описание
├── DOCUMENTATION.md        # Эта документация
│
├── src/                    # Исходный код (модульная структура)
│   ├── js/
│   │   ├── app.js          # Главный модуль инициализации
│   │   └── modules/
│   │       ├── inventory.js    # Управление инвентарём
│   │       ├── quests.js       # Система квестов
│   │       ├── cases.js        # Кейсы (рендер, открытие)
│   │       ├── settings.js     # Настройки (Steam, магазин)
│   │       ├── admin.js        # Админ-панель
│   │       └── navigation.js   # Навигация между вкладками
│   └── css/
│       └── main.css        # Все CSS стили
│
├── backup/                 # Резервные копии
│   ├── index_backup_*.html
│   └── app.bundle_backup_*.js
│
└── archive/                # Устаревшие/архивные файлы
```

---

## 🎯 Описание модулей

### `src/js/app.js` - Главный модуль
**Назначение:** Инициализация приложения и основные классы

**Классы:**
- `WeightedRandom` — взвешенный случайный выбор скинов
- `CarouselSpinner` — анимация карусели
- `Player` — управление балансом и инвентарём
- `CS2App` — главный контроллер приложения

---

### `src/js/modules/inventory.js` - Инвентарь
**Назначение:** Управление инвентарём игрока

**Ключевые функции:**
```javascript
renderInventory()           // Отрисовка инвентаря
toggleItemById(uid)         // Выбор/отмена предмета
sellItemById(uid)           // Продажа одного предмета
withdrawItemById(uid)       // Вывод одного предмета
sellItems()                 // Продать выбранные
withdrawItems()             // Вывести выбранные
toggleSelectAll()           // Выбрать/отменить всё
```

**Особенность:** Используется UID-система для предметов (уникальный идентификатор `_uid`), что предотвращает баги при удалении.

---

### `src/js/modules/quests.js` - Квесты
**Назначение:** Система ежедневных квестов

**Константы:**
```javascript
DAILY_POINTS_LIMIT = 20    // Макс. очков в день
CASE_COST = 100            // Стоимость кейса (5-6 дней)
```

**Ключевые функции:**
```javascript
getQuestData()             // Получить данные квестов
generateDailyQuests()      // Сгенерировать новые квесты
renderQuests()             // Отрисовать квесты
simulateQuestProgress()    // Симулировать прогресс
claimQuestReward(id)       // Забрать награду
```

---

### `src/js/modules/cases.js` - Кейсы
**Назначение:** Рендер кейсов и открытие

**Ключевые функции:**
```javascript
renderCases()              // Отрисовать все кейсы
openCase(caseId)           // Открыть кейс (с анимацией)
showCaseContents(caseId)   // Показать содержимое
closeCaseModal()           // Закрыть модал
```

---

### `src/js/modules/settings.js` - Настройки
**Назначение:** Привязка Steam, настройки магазина

**Ключевые функции:**
```javascript
getShopSettings()          // Получить настройки магазина
saveShopSettings(settings) // Сохранить настройки
linkSteam()                // Привязать Steam
updateSteamStatus()        // Обновить статус Steam
```

**localStorage ключи:**
- `steamLinked` — привязан ли Steam
- `shopSettings` — настройки магазина (`{sellEnabled, withdrawEnabled}`)

---

### `src/js/modules/admin.js` - Админ-панель
**Назначение:** Создание/редактирование кейсов

**Пароль админа:** `admin123`

**Ключевые функции:**
```javascript
checkAdminLogin()          // Проверка пароля
openAdminPanel()           // Открыть панель
renderAdminCaseList()      // Список кейсов
filterAdminSkins()         // Фильтрация скинов
saveAdminCase()            // Сохранить кейс
editCase(caseId)           // Редактировать кейс
deleteCase(caseId)         // Удалить кейс
```

---

### `src/js/modules/navigation.js` - Навигация
**Назначение:** Переключение вкладок, UI

**Ключевые функции:**
```javascript
openTab(tabName)           // Открыть вкладку
updateUI()                 // Обновить UI профиля
showNotification(msg)      // Показать уведомление
initNavigation()           // Инициализация
```

---

## 💾 Хранение данных (localStorage)

| Ключ | Описание |
|------|----------|
| `cs2_player_data` | Баланс и инвентарь игрока |
| `cs2_quests` | Данные квестов (дата, прогресс) |
| `customCases` | Пользовательские кейсы |
| `steamLinked` | Статус привязки Steam |
| `shopSettings` | Настройки магазина |
| `adminLoggedIn` | Сессия админа |

---

## 🎨 Система редкостей

| Редкость | Цвет | Базовая цена |
|----------|------|--------------|
| Consumer Grade | #b0c3d9 | 10 ⭐ |
| Industrial Grade | #5e98d9 | 25 ⭐ |
| Mil-Spec Grade | #4b69ff | 50 ⭐ |
| Restricted | #8847ff | 150 ⭐ |
| Classified | #d32ee6 | 400 ⭐ |
| Covert | #eb4b4b | 1000 ⭐ |
| Extraordinary | #ffcc00 | 2500 ⭐ |
| Contraband | #e4ae39 | 5000 ⭐ |

---

## 🔧 Добавление/изменение кейсов

### Через админ-панель (рекомендуется):
1. Откройте **Настройки** → **Администрирование**
2. Введите пароль `admin123`
3. Перейдите во вкладку **Создать**
4. Заполните форму и выберите скины
5. Нажмите **Сохранить кейс**

### Через код (app.bundle.js):
```javascript
// В методе initCases() класса CS2App
this.cases = [
  {
    id: 'my_custom_case',
    name: '🎁 Мой кейс',
    price: 200,
    description: 'Описание',
    skins: [...] // Массив скинов
  }
];
```

---

## 📱 Экраны приложения

1. **👤 Профиль**
   - Статистика игрока
   - CS2 статистика (фейк)
   - Ежедневные квесты
   - Инвентарь с продажей/выводом

2. **🎰 Кейсы**
   - Список кейсов
   - Открытие с анимацией карусели
   - Просмотр содержимого

3. **🏆 Топ**
   - Таблица лидеров (фейк)
   - Статистика сервера

4. **⚙️ Настройки**
   - Привязка Steam
   - Уведомления / Звуки
   - Сброс данных
   - Админ-панель

---

## 🛠️ Технические детали

### UID-система инвентаря:
Каждый предмет получает уникальный ID при добавлении:
```javascript
item._uid = `item_${Date.now()}_${idx}_${Math.random().toString(36).substr(2, 9)}`;
```
Это позволяет корректно удалять предметы без сдвига индексов.

### Система квестов:
- Квесты обновляются ежедневно
- Лимит 20 ⭐ в день
- Стоимость кейса: 100 ⭐ (5-6 дней накопления)

### Карусель:
- Длительность анимации: 6 секунд
- Используется cubic-bezier для плавности
- Целевой предмет вставляется заранее

---

## 🚀 Запуск

### Локально:
```bash
# Просто открой index.html в браузере
# Или запусти локальный сервер:
python -m http.server 8000
# или
npx serve .
```

### В Telegram:
1. Создать бота через @BotFather
2. Настроить Web App URL
3. Отправить кнопку с Web App

---

## 🔧 Отладка (консоль браузера)

```javascript
// Доступ к приложению
window.app

// Баланс игрока
window.app.player.balance

// Инвентарь
window.app.player.inventory

// Добавить баланс
window.app.player.balance += 10000
window.app.player.save()

// Получить все кейсы
window.app.getCases()

// Очистить данные
localStorage.clear()
location.reload()
```

---

## 📝 Changelog

### v2.0 (Текущая)
- ✅ Добавлена система квестов с дневным лимитом
- ✅ Исправлены баги инвентаря (UID-система)
- ✅ Добавлены кнопки продажи/вывода для каждого предмета
- ✅ Код разбит на модули в папке `src/`
- ✅ CSS вынесен в отдельный файл
- ✅ Создана система бэкапов

### v1.0
- Базовое открытие кейсов
- Анимация карусели
- Админ-панель
- Привязка Steam

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Очистите localStorage (Настройки → Сбросить данные)
3. Проверьте наличие `skins.json`
|----------|------|-------------------|
| Consumer Grade | #b0c3d9 | 79.92% |
| Industrial Grade | #5e98d9 | 15.98% |
| Mil-Spec Grade | #4b69ff | 3.2% |
| Restricted | #8847ff | 0.64% |
| Classified | #d32ee6 | 0.128% |
| Covert | #eb4b4b | 0.026% |
| Extraordinary | #e4ae33 | 0.002% |

---

## ⚠️ Важные заметки

1. **Изображения** — используй прямые ссылки (https://...)
2. **ID скинов** — должны быть уникальными
3. **Редкость** — поле `rarity.name` должно совпадать с ключами в `RARITY_CONFIG`
4. **После изменений** — обнови страницу (F5)

---

## 📝 Быстрый пример: Добавить кейс с 3 скинами

```javascript
// В app.bundle.js, строка ~500, внутри this.cases = [...]

{
  id: 'case_test',
  name: '🧪 Тестовый кейс',
  price: 50,
  description: 'Для тестирования',
  skins: [
    {
      id: 'test_1',
      name: 'AK-47 | Test Skin',
      rarity: { name: 'Covert', color: '#eb4b4b' },
      image: 'https://steamcdn-a.akamaihd.net/apps/730/icons/econ/default_generated/weapon_ak47_gs_ak47_empress_light_large.png',
      chance: 50
    },
    {
      id: 'test_2', 
      name: 'AWP | Test',
      rarity: { name: 'Classified', color: '#d32ee6' },
      image: 'https://steamcdn-a.akamaihd.net/apps/730/icons/econ/default_generated/weapon_awp_cu_awp_hyper_beast_light_large.png',
      chance: 30
    },
    {
      id: 'test_3',
      name: 'M4A4 | Test',
      rarity: { name: 'Restricted', color: '#8847ff' },
      image: 'https://steamcdn-a.akamaihd.net/apps/730/icons/econ/default_generated/weapon_m4a1_cu_m4a4_howl_light_large.png',
      chance: 20
    }
  ]
}
```

---

*Последнее обновление: Январь 2026*
